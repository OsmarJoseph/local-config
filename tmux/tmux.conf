set -g default-terminal "xterm-256color"
set -as terminal-features ",xterm-256color*:RGB"
set-window-option -g xterm-keys on

set -g prefix C-a
unbind C-b
bind-key C-a send-prefix

# bindings for moving words
bind-key -n M-Left send-keys M-b
bind-key -n M-Right send-keys M-f
bind-key -n º send-keys C-u
bind-key -n ¶ send-keys C-e
bind-key -n § send-keys C-a

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'folke/tokyonight.nvim'

source-file ~/.config/tmux/plugins/tokyonight.nvim/extras/tmux/tokyonight_night.tmux

set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0

set -g status-position top

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
